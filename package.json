{
  "name": "beefy-proto",
  "version": "0.5.0",
  "private": true,
  "dependencies": {
    "@openzeppelin/contracts": "^3.2.0",
    "bignumber.js": "^9.0.1",
    "keccak": "^3.0.1",
    "rlp": "^2.2.6"
  },
  "scripts": {
    "flat": "bin/flatten.sh",
    "clean": "rm -r artifacts cache",
    "net": "ganache-cli --fork ${BSC_RPC} --account=\"${GANACHE_PK}, ${GANACHE_BAL}\" --account=\"${GANACHE_OTHER_PK}, ${GANACHE_OTHER_BAL}\" --account=\"${HARDHAT_PK}, ${GANACHE_BAL}\"",
    "compile": "hardhat compile",
    "all": "yarn clean && yarn compile && yarn net",
    "test": "hardhat test --network localhost",
    "nonce": "node utils/logPredictedAddresses.js "
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.0.0",
    "hardhat": "^2.0.0",
    "mocha": "^8.2.1",
    "web3": "^1.3.0"
  }
}
